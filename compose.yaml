services:
  bot:
    build:
      context: .
    init: true
    restart: unless-stopped
    env_file:
      - .env.local
    environment:
      SQLITE_PATH: /data/usage.sqlite
    volumes:
      - bot-data:/data

volumes:
  bot-data:
